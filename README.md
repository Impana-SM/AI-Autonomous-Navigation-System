## NavOS Command Center

The project includes a browser-based Streamlit command center for mission monitoring, live simulation, route planning, perception review, route inspection, and run history.

```bash
pip install -r requirements.txt
streamlit run app.py
```

The original Pygame simulator remains available with `python main.py`.
